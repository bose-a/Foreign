# Uncomment the next line to define a global platform for your project
platform :ios, '11.3'

target 'Foreign' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Foreign
  #pod 'Moltin', '~> 3.0.6'
  pod 'Moltin','~> 3.0.7'
  pod 'TextFieldEffects'
  pod 'lottie-ios'
  pod 'Firebase/Database'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  target 'ForeignTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ForeignUITests' do
    inherit! :search_paths
    # Pods for testing
  end
end
